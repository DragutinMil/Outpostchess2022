/*const state = {};

const getters = {};

const mutations = {
    setNotification(state, notif_arr) {
        state.notif_arr = notif_arr;
    },
    receiveNotif(state, payload) {
        state.notif_arr = [payload, ...state.notif_arr];
        state.counterNot++;
    },
};

export default {
    state,
    getters,
    actions,
    mutations,
};
*/
